class AboutController < ApplicationController
# Here I am defining a method called index. It doesn't need to do anything at all because I am inheriting from the application controller. class AboutController < ApplicationController is saying if AboutController does anything then lets do that, but otherwise lets use the default rails renderer. The default rails renderer will go into the 

  def index

  end
end
